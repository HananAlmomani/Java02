var names = prompt("Enter Your name ")

document.write("Hello "+ names);
var makeupBrand = prompt("what brand do you want to look for ?");

while (makeupBrand != 'MN' && makeupBrand != 'sephora')
{
    makeupBrand = promot ("what brand do you want to look for ?");
    
}

